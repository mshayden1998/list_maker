export class Media {
    constructor(title, link, tags, mainTag, note) {
        this.title = title;
        this.link = link;
        this.tags = tags;
        this.mainTag = mainTag;
        this.note = note;
    }
}